generated: '2026-09-05'
method: generated
source: generated by the API Evangelist enrichment pipeline from the contracts in openapi/ and 4Paradigm's
  own REST reference; every operation named is verified present in the source it cites
name: 4Paradigm Agent Skills
note: 'Four skills covering the two surfaces an agent can actually drive. No skill is written for PhanthyMotus:
  its 124 actuator cards move physical hardware, none declares an undo or a safe-stop, and the per-tool
  inputSchemas are only available by live introspection against a running robot — authoring a motion skill
  from card names alone would be guessing at something that can hurt someone. 4Paradigm publishes no skills
  or AGENTS.md of its own; searched 2026-09-05.'
skills:
- file: 4paradigm-deploy-app-from-store.md
  name: deploy-openaios-app-from-store
  description: Find a Helm chart in the OpenAIOS app store, release it, and read back the running application's
    pods, services and notes.
  api: 4paradigm-openaios-platform
  operations:
  - getAppstoreChartList
  - getAppstoreChart
  - createRelease
  - getRelease
  - getApplicationList
  - getApplicationPods
  - getApplicationServices
  - getApplicationNotes
  - GetContainerLog
  - deleteRelease
  method: generated
- file: 4paradigm-manage-container-images.md
  name: manage-openaios-container-images
  description: Import container images into an OpenAIOS project, watch the import, cancel it, and manage
    the resulting image list.
  api: 4paradigm-openaios-platform
  operations:
  - ListImportingImages
  - POST /images/importing
  - PUT /images/importing
  - DELETE /images/importing
  - GET /images
  - PUT /images
  - DELETE /images
  - GET /images/registry
  - GET /images/info
  - GET /public_images
  method: generated
- file: 4paradigm-provision-dev-environment.md
  name: provision-openaios-dev-environment
  description: Provision, inspect and tear down a development environment on a self-hosted OpenAIOS-Platform
    deployment.
  api: 4paradigm-openaios-platform
  operations:
  - GetComputingUnitSpecs
  - createEnvironment
  - getEnvironment
  - getEnvironmentList
  - getTerminal
  - deleteEnvironment
  method: generated
- file: 4paradigm-serve-realtime-features.md
  name: serve-realtime-features-openmldb
  description: Insert rows, call a deployed real-time feature service, and run online or offline SQL against
    an OpenMLDB cluster over its REST APIServer.
  api: 4paradigm-openmldb-apiserver
  operations:
  - PUT /dbs/{db_name}/tables/{table_name}
  - POST /dbs/{db_name}/deployments/{deployment_name}
  - GET /dbs/{db_name}/deployments/{deployment_name}
  - POST /dbs/{db_name}
  - GET /dbs
  - GET /dbs/{db}/tables
  - POST /refresh
  method: generated
